# Bitácora nocturna — 2026-07-01

- **Rama**: `feat/night-agent/2026-07-01`
- **Commits realizados**: 9 / 20 (límite duro)
- **Estado del gate al cerrar**: lint ✅ · typecheck ✅ · test ✅ (60 tests) · build ✅ (8.9s)

> **Cierre 23:15** (usuario paró la sesión). Tras el resumen de abajo se añadieron 2 commits
> extra de playtest en vivo con el usuario: `7c59928` (brillo/contraste/sombras + registro
> de modelos por familia con auto-fit y fallback) y este. Verificado con Playwright headless:
> el juego arranca sin errores, las criaturas persiguen y pegan (HP 150→116 en el test).
>
> **Siguiente sesión, empezar por**: T-052b — descargar modelos CC0 (rata/lobo/esqueleto/
> monstruo/héroe) a `game/public/assets/models/creatures/` (el registro en
> `render/models.ts` ya los espera con esos nombres; el agente de descarga se quedó a medias,
> fuentes prometedoras: repo pmndrs/market-assets y packs KayKit en GitHub). Después: F3
> (T-030 tab-target, T-031 spells 1-4). El usuario reportó: "veo personajes de Star Wars"
> — correcto, es el placeholder Soldier hasta T-052b.

## Resumen ejecutivo (leer esto primero por la mañana)

- **Qué se completó**: F2 entera — T-020 (MeshPlan), T-021 (render instanciado con 4 temas),
  T-022 (escaleras + descenso con fade, HP se conserva), T-023 (catálogos → `game/public/data`,
  dataService TS), T-024 (spawning real: plantas 1–20 con sus criaturas y counts exactos del 2D,
  tintes por familia, velocidad por template).
- **Qué revisar primero**: jugar 2–3 plantas en `npm run dev` — bajar por la escalera dorada
  cambia tema/criaturas (Rats → Wolves → Rotworms…). Verificar sensación de dificultad:
  los stats vienen del catálogo real (una Vampire planta 14 pega MUY fuerte, como en el 2D).
- **PRs abiertos**: ⚠️ `gh` CLI no instalado — abrir manualmente:
  https://github.com/raulmaiz/tibia_dungeons/compare/feat/3d-migration...feat/night-agent/2026-07-01
- **Riesgos/avisos**:
  - Spawns capados a 30 rigs simultáneos por rendimiento (soldados skinned clonados);
    el sistema recorta proporcionalmente (planta 18: 50→30). Se levanta el cap con
    modelos instanciados (T-052b/T-060).
  - `game/public/data` pesa 113 MB (90 MB imágenes 2D heredadas). El build los copia a
    `dist/` (~9s). Purga de imágenes no usadas → candidata a tarea F6.

## Tareas

### Completadas
| ID | PR | Notas |
|----|----|-------|
| T-020 | (rama) | `buildMeshPlan` puro + 10 property-tests multi-seed |
| T-021 | (rama) | InstancedMesh por pieza+variante, <15 draw calls/planta, 4 temas |
| T-022 | (rama) | Fade + regeneración + dispose GPU; HP actual se conserva (paridad legacy) |
| T-023 | (rama) | `git mv` de catálogos (ruta /data intacta para middleware); dataService tipado |
| T-024 | (rama) | Tablas 1-20 byte-identical; 21+ cicla (→ T-025 nueva) |

### Intentadas sin terminar
| ID | Estado al parar | Motivo |
|----|-----------------|--------|
| — | — | — |

### Bloqueadas
| ID | Motivo del bloqueo | Pregunta concreta para el usuario |
|----|--------------------|------------------------------------|
| PRs | `gh` CLI ausente | ¿Instalo GitHub CLI (`winget install GitHub.cli`) contigo delante para autenticarlo? |

## Decisiones técnicas tomadas

- **Cap de 30 rigs por planta** (proporcional, mínimo 1 por especie) — documentado en
  `domain/spawning.ts`; el design intent (counts exactos) se restaura al instanciar modelos.
- **Plantas 21+ ciclan las tablas 1–20** en vez de portar el pooled-random del legacy esta
  noche (ambigüedad de alcance → tarea nueva T-025 en el backlog, no asumido).
- **Drop tables NO portadas** con T-023: dependen del pipeline de items (T-041, F4).
- **PR único por rama nocturna** mientras no exista `gh` (commits atómicos por tarea).

## Incidencias

- `git mv game/data` falló con Permission denied — el dev server de Vite bloqueaba la
  carpeta. Se paró el server, se movió, se relanzó. Anotado para futuras noches:
  parar el watcher antes de mover directorios grandes.
- Typecheck falló 1 vez (literal type de `as const` en camera.ts) — arreglado en el acto,
  dentro del margen de 2 intentos.
