{
  "name": "tibia-dungeons",
  "private": true,
  "scripts": {
    "dev":     "node scripts/dev-server.js",
    "build":   "node scripts/build.js --prod",
    "push":    "node scripts/bump-patch.js && git add game/js/data/version.js && git commit -m \"chore: bump patch version\" && git push",
    "release": "node scripts/bump-minor.js && git add game/js/data/version.js && git commit -m \"chore: release minor version\" && git push"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
